def open_or_senior(data):
    result = []
    for info in data:
        if info[0] >= 55 and info[1] > 7:
            result.append("Senior")
        else:
            result.append("Open")
    return result
